<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<%= link_to("Fuse out", destroy_user_session_path, method: :delete) %>

<ul>
  <% @users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>